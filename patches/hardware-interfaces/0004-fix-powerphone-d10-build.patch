diff --git a/audio/aidl/default/powerphone/Powerphone.cpp b/audio/aidl/default/powerphone/Powerphone.cpp
--- a/audio/aidl/default/powerphone/Powerphone.cpp
+++ b/audio/aidl/default/powerphone/Powerphone.cpp
@@ -1038,7 +1038,7 @@ ndk::ScopedAStatus StreamInPowerphone::start() {
         const bool exactGeometry =
                 mAlsaDeviceProxies.size() == 1 && mAlsaDeviceProxies.front().get() != nullptr &&
                 proxy_get_period_size(mAlsaDeviceProxies.front().get()) == 1920 &&
-                proxy_get_period_count(mAlsaDeviceProxies.front().get()) == 4;
+                mAlsaDeviceProxies.front().get()->alsa_config.period_count == 4;
         if (status != ::android::OK || !exactGeometry) {
             if (status == ::android::OK) {
                 LOG(ERROR) << __func__ << ": PCM 0,10 did not retain exact 1920x4 geometry";
diff --git a/audio/aidl/default/powerphone/Powerphone.h b/audio/aidl/default/powerphone/Powerphone.h
--- a/audio/aidl/default/powerphone/Powerphone.h
+++ b/audio/aidl/default/powerphone/Powerphone.h
@@ -119,7 +119,7 @@ class ModulePowerphone final : public Module {
             const ::aidl::android::media::audio::common::AudioPortConfig& portConfig) override;
     ndk::ScopedAStatus calculateBufferSizeFrames(
             const ::aidl::android::media::audio::common::AudioFormatDescription& format,
-            const ::aidl::android::hardware::audio::common::AudioIoFlags& flags, int32_t latencyMs,
+            const ::aidl::android::media::audio::common::AudioIoFlags& flags, int32_t latencyMs,
             int32_t sampleRateHz, int32_t* bufferSizeFrames) override;
 };

